$schema: 'https://moonrepo.dev/schemas/workspace.json'

projects:
  - 'apps/*'
  - 'packages/*'

vcs:
  client: 'git'
  defaultBranch: 'trunk'

constraints:
  tagRelationships:
    infra: ['infra']
    identity: ['infra']
    app: ['infra', 'identity']
