$schema: 'https://moonrepo.dev/schemas/project.json'

id: 'api-e2e'
layer: 'automation'
language: 'typescript'
stack: 'backend'

dependsOn:
  - 'api'

fileGroups:
  sources:
    - 'tests/**/*'
    - 'support/**/*'

tasks:
  e2e:
    command: 'pnpm e2e'
    options:
      runInCI: false

  typecheck:
    command: 'pnpm typecheck'
    inputs:
      - '@group(sources)'
      - 'tsconfig.json'
      - 'package.json'
